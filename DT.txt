 
Now I have a thorough understanding of the codebase patterns. Let me create the implementation plan:

SQL Functions — Add three new functions to database/estatehub.sql after fn_fy_closing_report (line 6721):
fn_receipts_payments_fy(p_society_id INT, p_fy INT)
fn_income_expenditure_fy(p_society_id INT, p_fy INT)
fn_balance_sheet_fy(p_society_id INT, p_fy INT)
Python Export Module — Create database/financial_statements_export.py with three export functions.
Loaders — Add three wrapper functions to app/dash_apps/drilldown/loaders.py.
Renderers — Add render_financial_statements_card to app/dash_apps/drilldown/renderers.py.
KPI Card — Add kpi_financial_statements to KPI_CARDS in app/dash_apps/pages/card_catalogue.py.
Callback — Add KPI click handler, render dispatch, and export dispatch in app/dash_apps/callbacks/drilldown_callbacks.py.
Let me also check the fn_resolve_bf_amount_fy function to understand the BF sign convention, and look at how the existing functions identify the Capital Account.

Actually, I already have enough context. Let me also check the fn_resolve_bf_amount_fy function to understand the BF sign convention, and look at how the existing functions identify the Capital Account.

Let me also look at the test file to understand expected behavior.

Let me now look at the fn_resolve_bf_amount_fy function and also understand the tab_name conventions by looking at the seed.py and the apexestatehub_ca_audit_fixes_prompt.md to understand the account structure. Also let me look at what the "Reports" section actually looks like in the UI - the design note says "under the Drilldown → Reports section".

Actually, let me check how the "Financials" section / "Reports" section is structured. Looking at the card_catalogue.py DEFAULT_LAYOUTS, I see there's a "financials" tab on the admin portal. But the design note mentions "Drilldown → Reports section". Let me look at how the drilldown is structured.

Actually, looking more carefully at the code:

The kpi_fy_closing_report card is in the "financials" group in DEFAULT_LAYOUTS When clicked, it navigates to form_fy_closing_report The render_fy_closing_card shows FY pills and export buttons So the "Financial Statements" card would follow the same pattern as kpi_fy_closing_report:

Add kpi_financial_statements KPI card to KPI_CARDS in card_catalogue.py Add click handler in drilldown_callbacks.py (similar to kpi_fy_closing_report) that navigates to form_financial_statements Add render_financial_statements_card in renderers.py Add _render_card dispatch for form_financial_statements Add download_fy_export dispatch for entity == "financial_statements" But wait - the design note says the card should have a "Format selector (xlsx / pdf)" and "Generate" button. The existing FY closing report uses the btn-fy-export pattern with dcc.Download. Let me follow that pattern.

Actually, looking at the render_fy_closing_card more carefully, the exports are buttons with id={"type": "btn-fy-export", "entity": "X"} and dcc.Download(id={"type": "fy-export-trigger", "entity": "X"}). The download_fy_export callback catches the button click and dispatches to the right export function.

For the Financial Statements, I'll follow the same pattern:

Add export buttons for Receipts & Payments, Income & Expenditure, Balance Sheet, and All Three Each uses the btn-fy-export / fy-export-trigger pattern Now, let me understand the SQL functions needed.

For the three SQL functions, I need to work with fn_fy_closing_report which returns:

account_id, account_name, tab_name, parent_account_id, drcr_account, has_bf, own_bf, own_movement, depreciation_charge, own_closing, total_closing, display_side, display_amount, depth, sort_path The sign convention is Cr-positive:

own_bf: Cr-positive (Cr BF → positive, Dr BF → negative) own_movement: Cr-positive (Cr receipts - Dr expenses) own_closing: Cr-positive (own_bf + own_movement + depreciation_charge) total_closing: Cr-positive (includes subtree rollup) display_amount: ABS(total_closing) display_side: 'Cr' if total_closing >= 0, 'Dr' if < 0 For classifying accounts as P&L vs. Balance Sheet:

The "Income Expenditure A/c" has tab_name='InExp' But P&L leaf accounts don't have tab_name='InExp' — they're descendants of account 23 However, looking at the accounts table, accounts 21, 22, 24, 25, 26, 27, 28, 29 are also under Capital Account (parent=2), but they're NOT under Income Expenditure A/c (parent=23) Actually wait, let me re-read the accounts structure:

Account 2 (Capital Account, parent=1, tab='CapAc') is under the root (account 1) Account 23 (Income Expenditure A/c, parent=2, tab='InExp') is under Capital Account Account 21 (Income Other Source, parent=2, tab='IncOther') is also under Capital Account Account 22 (Gifts Received, parent=2, tab='Gifts') is also under Capital Account So ALL P&L accounts are descendants of Capital Account (account 2). The Income Expenditure A/c (account 23) is just one child of Capital Account - it's the main income/expense rollup, but other children like Income Other Source (21), Gifts Received (22), Duties Paid (24), etc. are also P&L accounts under Capital Account.

For the SQL functions, I'll classify accounts based on their position relative to the Capital Account:

P&L accounts = descendants of Capital Account (identified by name) Balance Sheet accounts = everything else (under the root but not under Capital Account) Equity = Capital Account itself (account 2) The approach in the SQL functions:

Call fn_fy_closing_report to get all accounts with their closing balances Identify the Capital Account (by name = 'Capital Account' or tab_name = 'CapAc') Classify accounts: If account is a descendant of Capital Account (via parent_account_id chain) → P&L If account IS the Capital Account → Equity Otherwise → Balance Sheet For the receipts & payments, I need to look at actual cash/bank transactions. Looking at the fn_fy_closing_report:

Opening balances come from own_bf (which is -fn_resolve_bf_amount_fy(...) for has_bf accounts) Movement comes from own_movement (transactions) But for the Receipts & Payments account, I need actual cash/bank transaction lines, not just the summary. The fn_fy_closing_report doesn't give me individual transaction details.

Actually, re-reading the design note:

For fn_receipts_payments_fy:

"Start with fn_fy_closing_report opening balances (Dr/Cr)" "Add all cash/bank receipts (mode in cash, cheque, upi, card, bank, crypto) as Receipt lines (Cr to income accounts, Dr to cash/bank)" "Add all cash/bank expenses as Payment lines (Dr to expense accounts, Cr to cash/bank)" "Include journal entries marked mode='journal' as internal transfers" "End with closing balances from fn_fy_closing_report" So the design note says to use fn_fy_closing_report for opening/closing balances, AND to query the actual transactions for receipts and payments. Let me look at how fn_cashbook_paired_v3 queries transactions.

From the code, fn_cashbook_paired_v3 uses:

SELECT ... FROM transactions t WHERE t.society_id = p_society_id AND t.status = 'paid' AND t.mode = 'cash' AND t.trx_date BETWEEN v_fy_start AND v_fy_end For the Receipts & Payments account, I need:

Receipts: entry_side='Cr' AND mode IN ('cash','cheque','upi','card','bank','crypto') AND status='paid' Payments: entry_side='Dr' AND mode IN ('cash','cheque','upi','card','bank','crypto') AND status='paid' Now, for the SQL function fn_receipts_payments_fy, I need to return rows with:

line_type: 'Opening', 'Receipt', 'Payment', 'Closing' account_code, account_name dr_amount, cr_amount The Opening balance would be the CiH + Bank account opening balances (from fn_fy_closing_report's own_bf). The Receipt lines would be individual cash/bank receipt transactions (Cr side). The Payment lines would be individual cash/bank payment transactions (Dr side). The Closing balance would be the CiH + Bank account closing balances.

Hmm, but the design note says to start with fn_fy_closing_report opening balances and end with closing balances. Let me think about this more carefully.

Actually, for a Receipts & Payments account:

Opening balance: The total of all cash/bank accounts' opening balances Receipts: Total money received via cash/bank during the period Payments: Total money paid via cash/bank during the period Closing balance: The total of all cash/bank accounts' closing balances These can be aggregated as:

Opening: SUM(own_bf) for all cash/bank accounts (CiH, BkAc children) Receipts: SUM of all Cr-side cash/bank transactions Payments: SUM of all Dr-side cash/bank transactions Closing: SUM(own_closing) for all cash/bank accounts But the design note says the function should return individual lines with account_code and account_name. Let me think about what makes sense.

Actually, re-reading the design note's column spec:

line_type VARCHAR -- 'Opening', 'Receipt', 'Payment', 'Closing' account_code INT account_name VARCHAR dr_amount NUMERIC(15,2) cr_amount NUMERIC(15,2) And the logic says:

"Start with fn_fy_closing_report opening balances (Dr/Cr)" — this means Opening lines from fn_fy_closing_report "Add all cash/bank receipts" — Receipt lines from transactions "Add all cash/bank expenses" — Payment lines from transactions "End with closing balances from fn_fy_closing_report" — Closing lines from fn_fy_closing_report So the function returns a mix of:

Opening balance lines (from fn_fy_closing_report's own_bf for cash/bank accounts) Receipt transaction lines (individual cash/bank receipts) Payment transaction lines (individual cash/bank payments) Closing balance lines (from fn_fy_closing_report's own_closing for cash/bank accounts) Actually, I think a simpler approach that's more practical for the export would be to return aggregated/subtotaled rows. But let me look at how the existing exports work with raw transaction data.

Actually, looking at the design note again, it says the Receipts & Payments should use a "traditional two-column (Receipts left / Payments right) format per ICAI guidance." So it's not individual transactions, it's more of a summary account.

Let me re-think. The Receipts & Payments Account is a summary account that shows:

Opening balance of cash/bank All receipts (money received) during the period, categorized by account All payments (money paid) during the period, categorized by account Closing balance of cash/bank So the function should return:

Opening: aggregate opening balance of cash/bank accounts Receipt lines: each receipt amount grouped by the income/receiving account (Cr side) Payment lines: each payment amount grouped by the expense/paying account (Dr side) Closing: aggregate closing balance of cash/bank accounts Actually, for a proper Receipts & Payments account, the format is:

Receipts & Payments Account Dr side (Receipts/Money In) | Cr side (Payments/Money Out) Opening Balance Receipts:

Account 1: amount
Account 2: amount ... Total Receipts Payments:
Account 1: amount
Account 2: amount ... Total Payments Closing Balance The dr_amount and cr_amount columns correspond to the Dr and Cr columns of the account.
For the function, I think the most practical approach is:

For Receipts & Payments:

Opening: one line for each cash/bank account (CiH, BkAc children) with dr_amount or cr_amount depending on the balance side Receipt: one line for each distinct income/receiving account (Cr side of transactions with cash/bank mode) with dr_amount = amount Payment: one line for each distinct expense/paying account (Dr side of transactions with cash/bank mode) with cr_amount = amount Closing: one line for each cash/bank account with dr_amount or cr_amount depending on the balance side Actually, I think for a simplified implementation, I can aggregate:

Opening: total of all cash/bank accounts' opening balances Receipts: total of all cash/bank receipts (grouped by account) Payments: total of all cash/bank payments (grouped by account) Closing: total of all cash/bank accounts' closing balances Let me now think about how to identify "cash/bank accounts". From the seed:

CiH (633): Cash-in-hand, tab_name='CiH', drcr='Dr', has_bf=True SBI (6311): Bank account, tab_name='SBI', parent=631, drcr='Dr', has_bf=True ICICI (6312): Bank account, tab_name='ICICI', parent=631, drcr='Dr', has_bf=True BkAc (631): Bank Accounts, tab_name='BkAc', parent=63, drcr='Dr', has_bf=False Dp (632): Deposits (Assets), tab_name='Dp', parent=63, drcr='Dr', has_bf=True CurAs (63): Current Assets, tab_name='CurAs', parent=6, drcr='Dr', has_bf=False The cash/bank accounts are:

CiH (Cash-in-hand): directly has tab_name='CiH' Bank accounts: children of BkAc (account 631) — tab_name starts with 'BkAc' or is a child of account 631 Actually, looking at the accounts table, the header column might help:

CiH has header='Cash-in-hand' SBI/ICICI have header like 'SBI A/c - Society' But actually, the fn_fy_closing_report returns tab_name for each account. So I can identify cash/bank accounts by:

tab_name = 'CiH' (cash in hand) tab_name like 'SBI', 'ICICI', etc. (bank accounts) — these are children of BkAc (account 631) Or I could use the approach from fn_society_turnover_fy:

WHERE a.tab_name NOT IN ('BkAc', 'CiH', 'Dp', 'SCr') Wait, that's used to EXCLUDE these from income. So cash/bank accounts have tab_names like 'CiH', 'BkAc', 'SBI', 'ICICI', 'Dp' (deposits).

Actually, let me think about this differently. For the Receipts & Payments account, I need to look at ALL cash/bank transactions (receipts and payments), not just specific accounts. The transactions are already tagged with mode (cash, cheque, upi, card, bank, crypto, journal).

For receipts (entry_side='Cr'):

These are money received (income accounts credited) The cash/bank leg is the other side of the transaction For payments (entry_side='Dr'):

These are money paid (expense accounts debited) The cash/bank leg is the other side So for the Receipts & Payments account, I can query the transactions table directly:

Receipt lines: all transactions with entry_side='Cr' AND mode IN ('cash','cheque','upi','card','bank','crypto') AND status='paid' AND trx_date in FY, grouped by acc_id Payment lines: all transactions with entry_side='Dr' AND mode IN ('cash','cheque','upi','card','bank','crypto') AND status='paid' AND trx_date in FY, grouped by acc_id Opening/closing: from fn_fy_closing_report or fn_cih_balance_asof Wait, but the Receipts & Payments account is about the CASH/BANK accounts, not the income/expense accounts. The traditional R&P account shows:

What came in (receipts from various sources) What went out (payments to various parties) So the Receipt lines show the sources of cash (income accounts), and the Payment lines show the uses of cash (expense accounts).

For the Receipts & Payments account:

Receipts: money received from various sources (Cr to income accounts) → dr_amount = amount (money in) Payments: money paid to various parties (Dr to expense accounts) → cr_amount = amount (money out) So I should group transactions by the non-cash/bank account side:

Receipt lines: group by acc_id where entry_side='Cr' and the acc_id is NOT a cash/bank account (it's an income account) Payment lines: group by acc_id where entry_side='Dr' and the acc_id is NOT a cash/bank account (it's an expense account) Actually, let me look at the double-entry structure:

A receipt (income): Cr income account, Dr cash/bank account → two transaction rows with same journal_id An expense (payment): Dr expense account, Cr cash/bank account → two transaction rows with same journal_id So for the Receipts & Payments account:

Receipt side (dr_amount): income account rows where entry_side='Cr' (the money coming in) Payment side (cr_amount): expense account rows where entry_side='Dr' (the money going out) The cash/bank legs are:

For receipts: Dr cash/bank (entry_side='Dr' with cash/bank mode) For payments: Cr cash/bank (entry_side='Cr' with cash/bank mode) But in the R&P account, we show the income/expense accounts, not the cash/bank accounts. The opening and closing balances represent the cash/bank totals.

OK, let me simplify this. For the SQL function, I'll query transactions directly and group by account:

For Receipt lines:

SELECT 'Receipt' as line_type, t.acc_id as account_code, a.name as account_name, SUM(t.amount) as dr_amount, -- money in = debit side NULL as cr_amount FROM transactions t JOIN accounts a ON a.id = t.acc_id AND a.society_id = p_society_id WHERE t.society_id = p_society_id AND t.status = 'paid' AND t.trx_date BETWEEN v_fy_start AND v_fy_end AND t.entry_side = 'Cr' AND t.mode IN ('cash', 'cheque', 'upi', 'card', 'bank', 'crypto') AND a.drcr_account = 'Cr' GROUP BY t.acc_id, a.name Wait, but not all Cr-side transactions are income. Some could be returns of capital or similar. But for a simple implementation, I'll treat Cr-side transactions with cash/bank mode as receipts.

Actually, looking at this more carefully, I think the better approach is:

Receipt lines: transactions where entry_side='Cr' (money coming in) with cash/bank mode These are posted to income/receivable accounts (Cr-natured) dr_amount = SUM(amount) (since receipts increase the cash balance = debit in cash terms) Payment lines: transactions where entry_side='Dr' (money going out) with cash/bank mode These are posted to expense/asset accounts (Dr-natured) cr_amount = SUM(amount) (since payments decrease the cash balance = credit in cash terms) And for opening/closing:

From fn_fy_closing_report, get the CiH and bank accounts (those that participate in cash/bank movements) Opening: own_bf for each cash/bank account Closing: own_closing for each cash/bank account Actually, let me look at how fn_cashbook_paired_v3 identifies cash accounts. From the code:

It uses mode = 'cash' for the cash column fn_cih_balance_asof computes the cash-in-hand balance For the Receipts & Payments account, I need to identify cash/bank accounts. Let me look at what tab_names represent cash/bank:

'CiH': Cash-in-hand 'BkAc': Bank Accounts (the header/parent) 'SBI', 'ICICI': actual bank account leaves These are under 'BkAc'. So I need to find BkAc and all its descendants.

Let me use this approach in the SQL function:

-- Identify cash/bank accounts via fn_fy_closing_report WITH closing AS ( SELECT * FROM fn_fy_closing_report(p_society_id, p_fy) ), bkac AS ( SELECT id, sort_path FROM closing WHERE tab_name = 'BkAc' LIMIT 1 ), cih_accounts AS ( SELECT * FROM closing WHERE tab_name = 'CiH' ), bank_accounts AS ( SELECT c.* FROM closing c LEFT JOIN bkac ON TRUE WHERE c.sort_path LIKE bkac.sort_path || '.%' ) Actually, this is getting complex. Let me simplify by using a different approach. For the Receipts & Payments account:

The opening balance is the total of ALL cash/bank accounts' opening balances from fn_fy_closing_report I can identify cash/bank accounts by checking if their tab_name starts with 'CiH' or 'BkAc' or if they're bank sub-accounts But actually, looking at fn_cih_balance_asof, it only sums CiH accounts. For bank accounts, the BF is stored in brought_forward and computed via fn_fy_closing_report's own_bf.

Let me take a different, cleaner approach for the SQL functions. I'll:

For fn_receipts_payments_fy: Use the transactions table directly to get receipt/payment lines For opening/closing: identify cash/bank accounts via tab_name IN ('CiH', 'BkAc') or being a child of BkAc Actually, let me simplify even further. Looking at the design note's logic again:

"Start with fn_fy_closing_report opening balances (Dr/Cr)" "Add all cash/bank receipts (mode in cash, cheque, upi, card, bank, crypto) as Receipt lines" "Add all cash/bank expenses as Payment lines" "End with closing balances from fn_fy_closing_report"

The design note wants me to:

Use fn_fy_closing_report for opening/closing balances Use transactions directly for receipts/payments For opening balances, I can use fn_fy_closing_report's own_bf for all cash/bank accounts. But identifying cash/bank accounts requires knowing their tab_name.

Let me use a practical approach: For the R&P account, I'll identify cash/bank accounts as those where:

tab_name='CiH' (cash in hand) OR the account is a descendant of an account with tab_name='BkAc' (bank accounts) I can use the sort_path from fn_fy_closing_report to identify descendants of BkAc.

OR, even simpler: I can identify cash/bank accounts by looking at which accounts have transactions with non-journal modes. Since cash/bank accounts are where money physically moves, they'll have transactions with mode in ('cash','cheque','upi','card','bank','crypto').

Actually, the cleanest approach is to use the fn_fy_closing_report data and identify cash/bank accounts by their tab_name pattern. Let me look at what tab_names bank accounts have:

'BkAc': Bank Accounts (the header/parent) 'SBI', 'ICICI': actual bank account leaves These are under 'BkAc'. So I need to find BkAc and all its descendants.

Let me write the SQL to find these. In the fn_fy_closing_report output, I have sort_path and parent_account_id. I can find the BkAc account and then find all accounts whose sort_path starts with BkAc's sort_path.

OK, let me now write the actual SQL functions. I'll append them to estatehub.sql.

For the three functions, here's my plan:

fn_receipts_payments_fy CREATE OR REPLACE FUNCTION fn_receipts_payments_fy( p_society_id INT, p_fy INT ) RETURNS TABLE ( line_type VARCHAR, account_code INT, account_name VARCHAR, dr_amount NUMERIC(15,2), cr_amount NUMERIC(15,2) ) LANGUAGE plpgsql STABLE AS $$ DECLARE v_fy_start DATE := MAKE_DATE(p_fy, 4, 1); v_fy_end DATE := MAKE_DATE(p_fy + 1, 3, 31); BEGIN RETURN QUERY WITH closing AS ( SELECT * FROM fn_fy_closing_report(p_society_id, p_fy) ), bkac_base AS ( SELECT sort_path FROM closing WHERE tab_name = 'BkAc' LIMIT 1 ), cash_bank_accs AS ( SELECT c.account_id, c.account_name, c.own_bf, c.own_closing, c.display_side, c.display_amount FROM closing c CROSS JOIN bkac_base b WHERE c.tab_name = 'CiH' OR (b.sort_path IS NOT NULL AND c.sort_path LIKE b.sort_path || '.%') ) -- Opening balance lines SELECT 'Opening'::VARCHAR, acc.account_code::INT, acc.account_name::VARCHAR, CASE WHEN acc.own_bf >= 0 THEN acc.own_bf ELSE 0 END::NUMERIC(15,2) as dr_amount, CASE WHEN acc.own_bf < 0 THEN ABS(acc.own_bf) ELSE 0 END::NUMERIC(15,2) as cr_amount FROM ( SELECT account_id as account_code, account_name, own_bf as own_bf -- Cr-positive from fn_fy_closing_report FROM cash_bank_accs ) acc WHERE acc.own_bf != 0 UNION ALL -- Receipt lines (cash/bank receipts: Cr-side income accounts, non-journal mode) SELECT 'Receipt'::VARCHAR, t.acc_id::INT, a.name::VARCHAR, SUM(t.amount)::NUMERIC(15,2) as dr_amount, 0::NUMERIC(15,2) as cr_amount FROM transactions t JOIN accounts a ON a.id = t.acc_id AND a.society_id = p_society_id WHERE t.society_id = p_society_id AND t.status = 'paid' AND t.trx_date BETWEEN v_fy_start AND v_fy_end AND t.entry_side = 'Cr' AND t.mode IN ('cash', 'cheque', 'upi', 'card', 'bank', 'crypto') AND a.drcr_account = 'Cr' GROUP BY t.acc_id, a.name UNION ALL -- Payment lines (cash/bank payments: Dr-side expense accounts, non-journal mode) -- Wait, this can't have ORDER BY in a UNION branch in plpgsql... -- Actually, I need to handle this differently. Let me not use UNION ALL with ORDER BY in individual branches. -- I'll use a different approach. Hmm, I can't use ORDER BY in individual UNION branches in PostgreSQL. Let me restructure.

Actually, in PostgreSQL, you can't use ORDER BY in UNION branches. I need to wrap the whole thing in a subquery and ORDER BY outside.

Also, I need to think about how to handle NULL values in the dr_amount/cr_amount columns for opening/closing (where I have aggregated totals).

Let me also reconsider the R&P account structure. In a traditional R&P account:

The opening balance is a single aggregate of all cash/bank accounts Receipt lines are individual receipt entries grouped by account Payment lines are individual payment entries grouped by account The closing balance is a single aggregate But actually, looking at the design note's logic: "Start with fn_fy_closing_report opening balances (Dr/Cr)" - This could mean per-account opening balances, not aggregated. Let me re-read...

Actually, for a proper R&P account, the opening balance is the aggregate of all cash/bank accounts. Let me keep it as an aggregate.

But wait, for Receipt lines, the design note says "Cr to income accounts, Dr to cash/bank". In double-entry:

Receipt: Cr income account + Dr cash/bank The R&P account records the Dr side (cash/bank increase) in the Receipt column So for Receipt lines:

dr_amount = the amount received (the cash/bank leg, which is Dr) This should be grouped by the income account that received the money For Payment lines:

cr_amount = the amount paid (the cash/bank leg, which is Cr) This should be grouped by the expense account that the money was paid to Let me think about this differently. The transactions table has individual legs. For a receipt:

Leg 1: Dr cash/bank account (entry_side='Dr') Leg 2: Cr income account (entry_side='Cr') For the R&P account:

The Receipt line should show the income account and the amount dr_amount = amount (money received, increases cash) For a payment:

Leg 1: Cr cash/bank account (entry_side='Cr') Leg 2: Dr expense account (entry_side='Dr') For the R&P account:

The Payment line should show the expense account and the amount cr_amount = amount (money paid, decreases cash) So:

Receipt lines: SELECT FROM transactions WHERE entry_side='Cr' AND mode IN (cash modes) AND a.drcr_account='Cr' → dr_amount = SUM(amount) Payment lines: SELECT FROM transactions WHERE entry_side='Dr' AND mode IN (cash modes) AND a.drcr_account='Dr' → cr_amount = SUM(amount) This is what I had before. Let me restructure the function to avoid ORDER BY in UNION branches.

Actually, let me simplify the approach. Instead of a complex UNION, I'll build the result set using a simpler CTE structure:

RETURN QUERY WITH closing AS ( SELECT * FROM fn_fy_closing_report(p_society_id, p_fy) ), bkac_base AS ( SELECT sort_path FROM closing WHERE tab_name = 'BkAc' LIMIT 1 ), cash_bank_accs AS ( SELECT c.* FROM closing c CROSS JOIN bkac_base b WHERE c.tab_name = 'CiH' OR (b.sort_path IS NOT NULL AND c.sort_path LIKE b.sort_path || '.%') ), receipts AS ( SELECT 'Receipt'::VARCHAR as line_type, t.acc_id::INT as account_code, a.name::VARCHAR as account_name, SUM(t.amount)::NUMERIC(15,2) as dr_amount, 0::NUMERIC(15,2) as cr_amount FROM transactions t JOIN accounts a ON a.id = t.acc_id AND a.society_id = p_society_id WHERE t.society_id = p_society_id AND t.status = 'paid' AND t.trx_date BETWEEN v_fy_start AND v_fy_end AND t.entry_side = 'Cr' AND t.mode IN ('cash', 'cheque', 'upi', 'card', 'bank', 'crypto') AND a.drcr_account = 'Cr' GROUP BY t.acc_id, a.name ), payments AS ( SELECT 'Payment'::VARCHAR as line_type, t.acc_id::INT as account_code, a.name::VARCHAR as account_name, 0::NUMERIC(15,2) as dr_amount, SUM(t.amount)::NUMERIC(15,2) as cr_amount FROM transactions t JOIN accounts a ON a.id = t.acc_id AND a.society_id = p_society_id WHERE t.society_id = p_society_id AND t.status = 'paid' AND t.trx_date BETWEEN v_fy_start AND v_fy_end AND t.entry_side = 'Dr' AND t.mode IN ('cash', 'cheque', 'upi', 'card', 'bank', 'crypto') AND a.drcr_account = 'Dr' GROUP BY t.acc_id, a.name ) SELECT line_type, account_code, account_name, dr_amount, cr_amount FROM ( -- Opening SELECT 1 as sort_order, 'Opening'::VARCHAR as line_type, NULL::INT as account_code, 'Cash & Bank Balances b/f'::VARCHAR as account_name, COALESCE(SUM(CASE WHEN c.own_bf < 0 THEN ABS(c.own_bf) ELSE 0 END), 0)::NUMERIC(15,2) as dr_amount, COALESCE(SUM(CASE WHEN c.own_bf >= 0 THEN c.own_bf ELSE 0 END), 0)::NUMERIC(15,2) as cr_amount FROM cash_bank_accs c WHERE c.has_bf = TRUE HAVING COALESCE(SUM(CASE WHEN c.own_bf < 0 THEN ABS(c.own_bf) ELSE 0 END), 0) > 0 OR COALESCE(SUM(CASE WHEN c.own_bf >= 0 THEN c.own_bf ELSE 0 END), 0) > 0

UNION ALL

-- Receipts
SELECT 2 as sort_order, line_type, account_code, account_name, dr_amount, cr_amount FROM receipts

UNION ALL

-- Payments
SELECT 3 as sort_order, line_type, account_code, account_name, dr_amount, cr_amount FROM payments

UNION ALL

-- Closing
SELECT 4 as sort_order, 'Closing'::VARCHAR as line_type, NULL::INT as account_code,
       'Cash & Bank Balances c/f'::VARCHAR as account_name,
       COALESCE(SUM(CASE WHEN c.total_closing < 0 THEN ABS(c.total_closing) ELSE 0 END), 0)::NUMERIC(15,2) as dr_amount,
       COALESCE(SUM(CASE WHEN c.total_closing >= 0 THEN c.total_closing ELSE 0 END), 0)::NUMERIC(15,2) as cr_amount
FROM cash_bank_accs c
HAVING COALESCE(SUM(CASE WHEN c.total_closing < 0 THEN ABS(c.total_closing) ELSE 0 END), 0) > 0
    OR COALESCE(SUM(CASE WHEN c.total_closing >= 0 THEN c.total_closing ELSE 0 END), 0) > 0
) ordered ORDER BY sort_order, account_name; Wait, but the return type specifies account_code INT and I'm returning NULL::INT for opening/closing. That should be fine.

Actually, I realize I should also handle the case where the opening balance for cash/bank accounts uses the own_bf from fn_fy_closing_report. But for CiH, the own_bf is computed via fn_resolve_bf_amount_fy which looks at brought_forward. And for bank accounts (SBI, ICICI), same.

But there's a subtlety: fn_resolve_bf_amount_fy returns the BF in a specific sign convention:

For Dr-drcr accounts with Dr BF: returns +amount (positive) For Cr-drcr accounts with Cr BF: returns -amount (negative) Then fn_fy_closing_report negates it: own_bf = -fn_resolve_bf_amount_fy(...) So for a Dr-natured cash/bank account with a Dr opening balance:

fn_resolve_bf_amount_fy returns +amount own_bf = -amount (negative in Cr-positive convention) In the R&P account format:

own_bf < 0 → Dr balance → dr_amount = ABS(own_bf) = amount This is correct: a positive cash/bank balance is a Dr balance For an overdrawn bank account (Cr balance):

own_bf > 0 → Cr balance → cr_amount = own_bf This is correct Good, my conversion is right.

Now let me also think about whether I should use own_closing or total_closing for the individual cash/bank accounts. Since cash/bank accounts are leaf accounts (they don't have children in the account hierarchy - SBI/ICICI are leaves under BkAc), own_closing and total_closing would be the same for them. For BkAc itself (which is a header/parent), total_closing would include SBI + ICICI + any other bank accounts. I should use total_closing for BkAc to get the aggregate.

Actually, for the R&P account, the opening and closing should be the TOTAL of all cash/bank accounts, not per-account lines. So I should aggregate.

Let me revise:

For the R&P account:

Opening: single line showing total opening cash/bank balance dr_amount = SUM of ABS(own_bf) for cash/bank accounts with own_bf < 0 cr_amount = SUM of own_bf for cash/bank accounts with own_bf >= 0 Or equivalently: dr_amount = SUM(CASE WHEN own_bf < 0 THEN ABS(own_bf) ELSE 0 END), cr_amount = SUM(CASE WHEN own_bf >= 0 THEN own_bf ELSE 0 END) Receipt lines: grouped by income account, dr_amount = SUM(amount) These are Cr-side transactions (entry_side='Cr') with cash/bank mode on income accounts Payment lines: grouped by expense account, cr_amount = SUM(amount) These are Dr-side transactions (entry_side='Dr') with cash/bank mode on expense accounts Closing: single line showing total closing cash/bank balance Similar to opening but using own_closing/total_closing Actually, wait. Let me reconsider. In the traditional Receipts & Payments account:

The Dr column shows money received (receipts) The Cr column shows money paid (payments) Opening and closing balances are on whichever side they fall So:

Opening: if net cash/bank is a debit (Dr) balance → dr_amount; if credit (Cr/overdrawn) → cr_amount Receipts: each income account → dr_amount = amount received Payments: each expense account → cr_amount = amount paid Closing: if net cash/bank is a debit (Dr) balance → dr_amount; if credit (Cr/overdrawn) → cr_amount For receipts and payments, I need to think about what the Dr/Cr columns represent:

In a Receipts & Payments account, the Dr column lists money received, Cr column lists money paid So Receipt lines go in dr_amount (money in), Payment lines go in cr_amount (money out) This is clear. Let me now write the SQL functions.

For fn_receipts_payments_fy, I need to handle the CiH account specially because fn_fy_closing_report computes its own_bf via fn_resolve_bf_amount_fy (which looks at brought_forward), and its own_movement via the special CiH branch. But for the R&P account, I can use fn_cih_balance_asof for the opening/closing of CiH, and fn_fy_closing_report's total_closing for the aggregate.

Actually, let me just use fn_fy_closing_report for the opening/closing and transactions for the detail. This is what the design note specifies.

Let me now write the SQL functions. I'll also need to handle the CiH account's special opening balance. Looking at fn_cih_balance_asof:

v_bf = SUM of brought_forward bf_amount for CiH accounts (Dr: +amount, Cr: -amount) v_delta = SUM of mode='cash' transactions (Cr: +amount, Dr: -amount) from FY start to date RETURN v_bf + v_delta So fn_cih_balance_asof gives the CiH balance in the natural Dr-positive convention (positive = Dr balance).

For the R&P account:

Opening CiH balance = fn_cih_balance_asof(p_society_id, v_fy_start - 1) or fn_cih_balance_asof(p_society_id, v_fy_start) - transactions on v_fy_start Actually, looking at fn_cashbook_paired_v3's fix, the correct way is: v_opening_balance = fn_cih_balance_asof(p_society_id, v_fy_start) - (transactions on v_fy_start with mode='cash') But for bank accounts, the opening balance comes from brought_forward.

Hmm, this is getting complex. Let me simplify by using fn_fy_closing_report for ALL cash/bank accounts' opening and closing balances. fn_fy_closing_report computes:

own_bf: for has_bf=TRUE accounts, this is -fn_resolve_bf_amount_fy(p_society_id, a.id, p_fy) But for CiH, fn_resolve_bf_amount_fy looks at brought_forward directly Wait, but the CiH account in the seed has has_bf=TRUE. So fn_fy_closing_report would compute its own_bf from fn_resolve_bf_amount_fy, which looks at the brought_forward table for CiH.

But fn_cih_balance_asof computes the CiH balance differently - it includes the net cash movement from the current FY up to a date. For the opening balance at FY start, it would be just the BF (no current FY movement yet, unless there are transactions on the first day).

Actually, for the R&P account, the opening balance should be the cash/bank balance at the START of the FY (before any FY transactions). This is:

For CiH: BF at FY start (from brought_forward) For bank accounts: BF at FY start (from brought_forward) fn_fy_closing_report's own_bf gives exactly this - the brought-forward amount for the FY.

So I can use fn_fy_closing_report's own_bf for opening and total_closing for closing, for all cash/bank accounts.

But I need to identify which accounts are cash/bank. Let me use the approach of finding accounts with tab_name='CiH' or descendants of an account with tab_name='BkAc'.

OK, let me now write the actual SQL. Let me also think about the fn_income_expenditure_fy function.

For the Income & Expenditure account:

Income: P&L accounts with drcr='Cr' Expenditure: P&L accounts with drcr='Dr' Surplus/Deficit: Total Income - Total Expenditure P&L accounts are descendants of the Capital Account (tab_name='CapAc'). I can identify them using the sort_path from fn_fy_closing_report.

For the amounts, I should use own_closing for individual P&L accounts (since they're leaf accounts and own_closing = total_closing for leaves). But some P&L accounts might be parents (like account 21 "Income Other Source" which is a parent of 211 "Interest Income"). In that case, I should use individual leaf accounts or the aggregate.

Actually, for the I&E account, it's more traditional to show individual income and expense line items. I'll use leaf-level accounts (depth > 0 where no other account has this as parent) to avoid double-counting.

Wait, but the fn_fy_closing_report already returns aggregate values for parent accounts. If I show both parent and child accounts, I'd double-count. I should either:

Show only leaf accounts (use own_closing) Show only depth=1 P&L accounts (the direct children of Income Expenditure A/c) with their total_closing For the I&E account, option 2 is more traditional — showing the major categories. But the design note says "classify by account tab/drcr", suggesting individual accounts.

Let me use option 1: show leaf-level P&L accounts with their own_closing. For the amount:

Income (Cr-drcr): amount = own_closing (which is positive in Cr-positive convention for income) But own_closing could be negative if there are refunds/payments. Let me use ABS(own_closing) and display_side to determine the amount. Actually, for income accounts, if own_closing is positive (Cr balance), the amount is own_closing. If own_closing is negative (Dr balance, meaning expenses exceeded income on that account), the amount should still be shown but as a negative value or in the expenditure column. Hmm, this is getting complicated. Let me use display_amount and display_side from fn_fy_closing_report to determine the amount, similar to how _children_rollup_rows in ledger_export.py does it.

For the I&E account:

Income: P&L accounts with drcr='Cr' — amount = display_amount (which is ABS(total_closing)) But wait, if an income account has a Dr balance (more payments than receipts), display_side='Dr' and the amount would be shown in the expenditure column. This is an edge case. Actually, for the I&E account, the amount should reflect the net effect. For an income account:

If total_closing > 0 (Cr balance): it's income, amount = total_closing If total_closing < 0 (Dr balance): it's actually a loss/reversal, amount = ABS(total_closing), but it should probably be shown as negative income or as expenditure For simplicity, I'll use the approach from the design note:

Income: amount = ABS(total_closing) for Cr-drcr accounts under InExp Expenditure: amount = ABS(total_closing) for Dr-drcr accounts under InExp But this doesn't handle the edge case where a Cr account has a Dr balance or vice versa.

Actually, let me re-read the design note: "Income: All accounts under Income & Expenditure branch (tab='InExp' and drcr='Cr')" "Expenditure: All accounts under Income & Expenditure branch (tab='InExp' and drcr='Dr')"
